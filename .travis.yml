matrix:
  include:
    - language: python
      python: 3.6
      script:
          - cd python
          - pytest 
    - language: scala
      scala: 2.12.1
      script:
          - cd scala
          - for i in $( ls); do  cd $i; echo -e "\e[1;32m==========testing $i=========="; sbt ++$TRAVIS_SCALA_VERSION test; cd ..  ; done
